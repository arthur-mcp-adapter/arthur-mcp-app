upstream backend {
    server app:3000;
}

server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # API NestJS
    location /api/ {
        proxy_pass         http://backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    }

    # MCP endpoints (used by Claude, Cursor, etc.)
    location /mcp/ {
        proxy_pass         http://backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_read_timeout 300s;
    }

    location /health   { proxy_pass http://backend; }
    location /ready    { proxy_pass http://backend; }
    location /live     { proxy_pass http://backend; }
    location /metrics  { proxy_pass http://backend; }
    location /mcp-docs { proxy_pass http://backend; }

    # React SPA — returns index.html for all React Router routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}

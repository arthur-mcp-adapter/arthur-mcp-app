networks:
  backend:
    driver: bridge

volumes:
  sqlite_data:
    driver: local

services:

  # ── NestJS backend (API apenas) ──────────────────────────────────────────────
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-dashboard
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - SQLITE_PATH=/app/data/database.sqlite
    volumes:
      - sqlite_data:/app/data
    networks:
      - backend
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ── React frontend (nginx serve static + proxy para o backend) ───────────────
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: mcp-frontend
    ports:
      - "80:80"
    networks:
      - backend
    depends_on:
      app:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  # ── MongoDB (opcional — descomentar e definir DATABASE=mongodb no .env) ──────
  # mongo:
  #   image: mongo:7
  #   container_name: mcp-mongo
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: mcp
  #     MONGO_INITDB_ROOT_PASSWORD: mcp_password
  #     MONGO_INITDB_DATABASE: mcp_db
  #   volumes:
  #     - mongo_data:/data/db
  #   networks:
  #     - backend
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
  #     interval: 30s
  #     timeout: 5s
  #     retries: 3
  #     start_period: 10s
  #
  # volumes:
  #   mongo_data:
  #     driver: local
